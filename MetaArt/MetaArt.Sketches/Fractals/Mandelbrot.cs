namespace Fractals;
class Mandelbrot : SketchBase {
    /**
     * The Mandelbrot Set
     * by Daniel Shiffman.  
     * (slight modification by l8l)
     *
     * Simple rendering of the Mandelbrot set.
     */
    void setup() {
        size(640, 360);
        noLoop();
        background(0);
    }

    void draw() {
    }
}
